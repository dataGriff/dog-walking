meta {
  name: Login Walker
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "jane.walker@example.com",
    "password": "password123"
  }
}

script:post-response {
  bru.setVar("walkerRefreshToken", res.getBody().refreshToken);
  bru.setVar("walkerToken", res.getBody().accessToken);
}
