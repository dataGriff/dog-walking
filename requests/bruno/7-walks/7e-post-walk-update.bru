meta {
  name: Post Walk Update
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/walks/{{walkId}}/updates
  body: json
  auth: bearer
}

auth:bearer {
  token: {{walkerToken}}
}

body:json {
  {
    "type": "note",
    "note": "Buddy is doing great — just passing Bute Park!"
  }
}
