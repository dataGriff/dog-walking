meta {
  name: Create Owner
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/owners
  body: json
  auth: bearer
}

auth:bearer {
  token: {{walkerToken}}
}

body:json {
  {
    "firstName": "John",
    "lastName": "Owner",
    "email": "john.owner2@example.com",
    "phone": "02920123456"
  }
}

script:post-response {
  bru.setVar("ownerId", res.getBody().id);
}
