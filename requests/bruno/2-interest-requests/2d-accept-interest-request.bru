meta {
  name: Accept Interest Request
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/interest-requests/{{interestRequestId}}/accept
  body: json
  auth: bearer
}

auth:bearer {
  token: {{walkerToken}}
}

body:json {
  {
    "notes": "Looking forward to meeting Buddy!"
  }
}
